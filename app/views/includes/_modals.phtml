<?php if (isset($this->events)) : ?>
  <?php foreach ($this->events as $event) : ?>
    <div id="event<?= $event->getIdEvent();?>-modal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-body">
          	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          	<div class="event col-lg-12 col-md-12 col-sm-12 col-xs-12">
            	<img class="event-thumbnail" src="<?= $this->getResource('img/placeholder-evento.png'); ?>" alt="...">
            </div>
            <div class="caption">
    	        <h2><?= $event->getName() ?></h2>
               <?php if ($event->getStartDate() == $event->getEndDate()) : ?>
                <h4> <?= $event->getStartDate() ?></h4>
              <?php else : ?>
                <h4>De <?= $event->getStartDate() ?> a <?= $event->getEndDate() ?></h4>
              <?php endif; ?>

              <?php if ($event->getStartTime() == $event->getEndTime()) : ?>
                <h5> <?= $event->getStartTime() ?></h4>
              <?php else : ?>
                <h5>Das <?= $event->getStartTime() ?> às <?= $event->getEndTime() ?></h4>
              <?php endif; ?>
    	        <p>Acesse a página do evento para mais detalhes.</p>
    	      </div>
    	      <a href="#" class="btn btn-default" role="button">Ir para a página do evento</a>
            <?php if (date("Y-m-d") >= $event->getStartDateEnrollment() && date("Y-m-d") <= $event->getEndDateEnrollment()) : ?>
    	       <a role="button" class="btn btn-default" href="<?= createUriFor("/inscricao/evento/{$event->getIdEvent()}")?>">Inscrever-se</a>
            <?php endif; ?>
    	      <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
          </div>
        </div>
      </div>
    </div>
  <?php endforeach; ?>
<?php endif; ?>