<div class="col-xs-12 col-sm-12 col-md-10 col-md-offset-1 col-lg-10 col-lg-offset-1 users-list" style="">
	<div class="panel panel-default">
		<div class="panel-heading">
	    <h3 class="panel-title">Lista de participantes cadastrados</h3>
	  </div>
	  <div class="panel-body">
	  	<span class="glyphicon glyphicon-question-sign" style="float:right;"></span>
			<div class="row">
				<div class="col-xs-12 general-actions">
					<a href="<?=$this->getUri("admin/participantes/novo")?>" role="button" class="btn btn-success btn-sm" title="Cadastrar" style="float: left"><span class="glyphicon glyphicon-plus-sign"></span></a>
				</div>
			</div>
		</div>
		<table class="table">
			<tr>
				<th>ID</th>
				<th>Nome</th>
				<th>E-mail</th>
				<th>Categoria</th>
				<th>Ações</th>
			</tr>
			<?php foreach ($this->participants as $participant) : ?>
				<tr class="<?= $participant->getEnabled() ? '' : 'warning' ?>">
					<td><?=$participant->getIdParticipant()?></td>
					<td align="left"><?=$participant->getName()?></td>
					<td align="left"><?=$participant->getEmail()?></td>
					<td><?= $participant->participantType->getName() ?>
						<?php if (!$participant->getEnabled()): ?>
							<span class="badge pull-right"><span class="glyphicon glyphicon-hourglass" title="Aguarda aprovação"></span></span>
						<?php endif ?>
					</td>
					<td>
						<a href="<?=$this->getUri("admin/participantes/{$participant->getIdParticipant()}/alterar")?>" role="button" class="btn btn-primary btn-sm" title="Editar"><span class="glyphicon glyphicon-edit"></span></a>
						<a href="<?=$this->getUri("admin/participantes/{$participant->getIdParticipant()}/remover")?>" role="button" class="btn btn-danger btn-sm" title="Excluir"><span class="glyphicon glyphicon-trash"></span></a>
						<?php if ($participant->getEnabled()): ?>
							<a href="<?=$this->getUri("admin/participantes/{$participant->getIdParticipant()}/desativar")?>" role="button" class="btn btn-material-deep-purple btn-sm" title="Bloquear Participante"><span class="glyphicon glyphicon-ban-circle"></span></a>
						<?php else: ?>
							<a href="<?=$this->getUri("admin/participantes/{$participant->getIdParticipant()}/ativar")?>" role="button" class="btn btn-success btn-sm" title="Aprovar Participante"><span class="glyphicon glyphicon-ok-circle"></span></a>
						<?php endif; ?>
					</td>
				</tr>
			<?php endforeach ?>
		</table>
		<div class="panel-footer">
			<div class="row">
				<div class="col-xs-12">
					<ul class="pagination">
					  <?php echo $this->pagination->getPageAnchors("{$this->getUri('admin/participantes/lista')}");?>
					</ul>
					<br/>
				</div>
			</div>
		</div>
	</div>
</div>