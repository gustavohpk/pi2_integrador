<?php 
	class EnrollmentController extends BaseController{
   		public function _list() {
      		$this->setHeadTitle("Lista de Inscrições");
   		}

   		public function _new(){
   			$this->setHeadTitle("Criar inscrição");
            $dado = array("id_enrollment"=>1, "id_participant"=>1, "id_event"=>7);
            $teste = new Enrollment($dado);
         

            //unset($_SESSION["cart"]);
            $cart = new Cart();
            $cart->add(1);
            //$cart->close();
            echo "<pre>"; print_r($cart->getItems()); echo "<br><br>Total: " . $cart->getSum();

   		}

   		public function _edit(){
   			$this->setHeadTitle("Modificar inscrição");
   		}
	} 
?>